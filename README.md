# regex.c
A simple C lib of regex.  
The mini branch only retains the core operators:`&` `|` `*` `.` `(` and `)`.  

## Quick Start
```
cc regex.c test.c -o test.out && ./test.out  
```

## Arch
![arch](arch.svg)
